# Real Estate React + Tailwind Sample

Quick start:
1. unzip and `cd` into the project
2. run `npm install`
3. run `npm run dev`
4. open the dev server (default: http://localhost:5173)

This is a minimal Vite + React + Tailwind starter with a small demo UI.
